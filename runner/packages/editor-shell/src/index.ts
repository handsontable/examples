// Framework-agnostic editor shell. The full UI (file tree, code editor, preview
// slot, toolbar, version picker, Share) lands in Deliverable 3; for now the
// package exposes the branding tokens the apps consume.
export { theme } from "./theme.js";
export type { Theme } from "./theme.js";
